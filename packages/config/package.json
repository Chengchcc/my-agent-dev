{
  "name": "@chengchenccc/config",
  "version": "0.1.0",
  "description": "Shared envSchema + parseEnv \u2014 single source of truth for all process environment variables",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "biome check . && eslint .",
    "test": "bun test --pass-with-no-tests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {},
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Chengchcc/my-agent-team.git",
    "directory": "packages/config"
  }
}
