# Superpowers 归档区

> ⚠ **归档标注（2026-08-21，T6）**：本目录是**历史归档**，specs / plans / retros 记录已发生或已否决的设计推演，**不代表当前架构**。阅读现状与决策请以活区为准：

- 现状：`docs/architecture/`（含 `index.llm.md` 入口 + `MANIFEST.md` 打包清单）
- 决策：`docs/adr/`（ADR 索引 `README.md` 是决策唯一目录）

## 内容分区

| 目录 | 性质 |
|---|---|
| `specs/` | 历史设计 spec（含 `agent-backend-coding-agent-rewrite/` 分卷） |
| `plans/` | 历史实施计划（含归档的 `loop-engineering.md` PRD） |
| `retros/` | 历史复盘 |

**维护规则**：新设计推演请勿写入本目录（放 `docs/architecture/` 或 ADR）；历史页可保留旧链接/旧术语（归档豁免 `audit:docs` 的死链与词表检查）。
